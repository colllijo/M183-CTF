spring:
  datasource:
    url: jdbc:h2:mem:ctf;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    hiberrnative:
      ddl-auto: create
    open-in-view: false
  liquibase:
    drop-first: true
